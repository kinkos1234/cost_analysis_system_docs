# 08-B. CAS_Calculation_Engine_Unit_n_Property_Test_Spec

## 1. Purpose

계산 엔진(04문서)과 비용 사전(Catalog)을 **Unit/Property 기반 TDD**로 검증하기 위한 테스트 규격(L1/L2)이다.

아래 문서를 테스트로 검증한다.

* 04. CAS_Calculation_Engine_n_Cost_Rule_Spec
* 05. CAS_Target_n_Evaluation_Logic_Spec (평가 입력값 전제 검증)
* 08-A. CAS_Domain_Invariant_Test_Spec (불변식 준수)

---

## 2. Test Levels & Conventions

### 2.1 Levels

* **L1 Unit Tests:** Pure function 단위(입력→출력) 테스트
* **L2 Property Tests:** 입력 범위를 확장해 성질(항상 참)을 검증

> **v1.3 변경:** v1.2의 L3(Ontology Parity Tests) 제거. SWRL 추론 엔진이 제거되었으므로 Parity 검증 불필요. 대신 L2 Property Tests를 강화.

### 2.2 Notation

* per-unit 값은 소수/정수 모두 허용
* 금액(amount)은 원 단위 integer로 표현(부동소수점 금지)
* 5Y 평균은 단순 평균(AVERAGE)

---

## 3. Calculation Unit Tests (L1)

### 3.1 Standard Aggregation (per-unit)

#### UT-CALC-001: TotalCost(per-unit)

**Given** M=60, A=10, L=5, K=2, C=0 (관세)
**When** total_cost = M+A+L+K+C
**Then** total_cost = 77

> **v1.3 변경:** 관세(C) 항목 추가. 값이 0이어도 산식에 포함.

#### UT-CALC-002: TotalRevenue(per-unit)

**Given** PRICE=100
**Then** total_revenue = 100

#### UT-CALC-003: CostRatio(per-unit)

**Given** total_cost=77, total_revenue=100
**Then** cost_ratio = 0.77

#### UT-CALC-004: TotalCost with CUSTOMS

**Given** M=60, A=10, L=5, K=2, C=3
**When** total_cost = M+A+L+K+C
**Then** total_cost = 80

> **v1.3 추가:** 관세가 0이 아닌 경우의 총비용 계산 검증.

---

### 3.2 Amount Conversion (yearly)

#### UT-AMT-001: RevenueAmount(year)

**Given** price_per_unit[Y1]=100, volume[Y1]=10,000
**Then** revenue_amount[Y1] = 1,000,000

#### UT-AMT-002: CostAmount(year)

**Given** mat_per_unit[Y1]=60, volume[Y1]=10,000
**Then** mat_amount[Y1] = 600,000

#### UT-AMT-003: TotalRevenue(amount)

**Given** revenue_amount[Y1..Y5]
**Then** total_revenue_amount = SUM(revenue_amount[Y1..Y5])

#### UT-AMT-004: TotalCost(amount)

**Given** selected_cost_component_amounts[year]
**Then** total_cost_amount = SUM(cost_amounts[year]) aggregated across years

#### UT-AMT-005: Multi CR Revenue

**Given** 초도가=100, CR%=3%, volume[Y1..Y3]=[10000, 10000, 10000]
**Then** revenue[Y1]=100×10000=1,000,000
**Then** revenue[Y2]=97×10000=970,000
**Then** revenue[Y3]=94.09×10000=940,900

> **v1.3 추가:** Multi CR 적용 연도별 매출 금액 검증.

---

### 3.3 5Y Average View (per-unit)

#### UT-AVG5-001: Avg5(price)

**Given** price[Y1..Y5] = [100, 100, 90, 90, 80]
**Then** avg5_price = AVERAGE = 92

#### UT-AVG5-002: Avg5(cost item)

**Given** mat[Y1..Y5] = [60, 61, 62, 63, 64]
**Then** avg5_mat = 62

---

### 3.4 Extended Catalog Components

#### UT-EXT-A-001: A-Type Rate×Revenue

**Given** rate=0.05, revenue_amount[Y1]=10,000,000
**Then** component_amount[Y1] = 500,000

#### UT-EXT-B-001: B-Type per-unit×Volume

**Given** per_unit=77, volume[Y1]=10,000
**Then** component_amount[Y1] = 770,000

#### UT-EXT-C-001: C-Type Allocation required

**Given** component_type=C and included=ON and allocation missing
**Then** error_code = PROGRAM_ALLOCATION_MISSING

#### UT-EXT-C-002: C-Type Allocation applied

**Given** allocation[Y1..Y5] provided
**Then** component_amount[year] = allocation[year]

---

### 3.5 Rounding & Precision

#### UT-ROUND-001: CostRatio precision

**Rule:** CAS v1은 CostRatio를 내부적으로 high precision(decimal)로 유지하고, UI 출력 시 4자리 반올림 표준을 사용한다.
**Given** total_cost=1, total_revenue=3
**Then** internal=0.333333..., display=0.3333

---

### 3.6 Derived Metric Unit Tests

> **v1.3 추가:** v1.2에서 SWRL 형식화가 누락되었던 파생 지표를 순수 함수 Unit Test로 검증.

#### UT-DRV-001: Manufacturing Cost Ratio

**Given** MAT=60, ASM=10, LABOR=5.1, MFG_OH=3.0, DEP_MFG=2.0, DISPATCH=1.0, REVENUE=100
**Then** mfg_cost = 60+10+5.1+3.0+2.0+1.0 = 81.1
**Then** mfg_cost_ratio = 81.1/100 = 0.811

#### UT-DRV-002: Cost of Sales Ratio

**Given** mfg_cost=81.1, LOG=5, PKG=2, CUSTOMS=0, REVENUE=100
**Then** cost_of_sales = 81.1+5+2+0 = 88.1
**Then** cos_ratio = 88.1/100 = 0.881

#### UT-DRV-003: Reduction Target Reverse Calc (1안)

**Given** REVENUE=100, target_ratio=0.80
**Then** target_total_cost = 100×0.80 = 80
**Given** 현재_MAT=60(비중 68.2%), 현재_ASM=10(비중 11.4%)
**Then** 목표_MAT = 80×0.682 = 54.56
**Then** MAT_절감율 = (60-54.56)/60×100 = 9.07%

---

### 3.7 Part → Vehicle Aggregation Unit Tests

> **v1.3 추가:** 품번 → 차종 집계 규칙(04 §8) 검증.

#### UT-AGG-001: Simple Aggregation (usage=1)

**Given** Part1(MAT=30, usage=1), Part2(MAT=30, usage=1)
**Then** Vehicle_MAT = 30+30 = 60

#### UT-AGG-002: Weighted Aggregation (usage>1)

**Given** Part1(MAT=30, usage=2), Part2(MAT=20, usage=1)
**Then** Vehicle_MAT = 30×2 + 20×1 = 80

#### UT-AGG-003: Aggregation Mismatch Detection

**Given** Vehicle_MAT(입력)=60, SUM(Part_MAT×usage)=65
**Then** error_code = AGGREGATION_MISMATCH

---

### 3.8 Evaluation Unit Tests

> **v1.3 추가:** v1.2에서 SWRL-EVAL로 검증하던 평가 로직을 순수 함수 Unit Test로 검증.

#### UT-EVAL-001: GREEN Status

**Given** metricValue=0.78, targetValue=0.80, warningThreshold=0.02
**Then** status = GREEN

#### UT-EVAL-002: YELLOW Status

**Given** metricValue=0.81, targetValue=0.80, warningThreshold=0.02
**Then** status = YELLOW

#### UT-EVAL-003: RED Status

**Given** metricValue=0.83, targetValue=0.80, warningThreshold=0.02
**Then** status = RED

#### UT-EVAL-004: Boundary GREEN-YELLOW

**Given** metricValue=0.80, targetValue=0.80, warningThreshold=0.02
**Then** status = GREEN (경계값은 달성으로 판정)

#### UT-EVAL-005: Boundary YELLOW-RED

**Given** metricValue=0.82, targetValue=0.80, warningThreshold=0.02
**Then** status = YELLOW (경계값은 YELLOW로 판정)

---

## 4. Property Tests (L2)

### 4.1 Domain Range Properties

#### PT-RANGE-001: Core costs non-negative

**For all** M,A,L,K,C in [0..MAX]
**Then** total_cost >= 0

#### PT-RANGE-002: Revenue positive

**For all** PRICE in (0..MAX]
**Then** total_revenue > 0

---

### 4.2 Algebraic Properties

#### PT-ALG-001: Additivity

**For all** M1,M2,A,L,K,C
**Then** total_cost(M1+M2,A,L,K,C) = total_cost(M1,A,L,K,C)+M2

#### PT-ALG-002: Ratio monotonicity (holding revenue)

**For all** revenue fixed, cost increases
**Then** cost_ratio increases

#### PT-ALG-003: Multi CR Monotonic Decrease

**For all** CR% in [0..1], year N ≥ 2
**Then** 판매가[N] ≤ 판매가[N-1]

> **v1.3 추가:** INV-CALC-005 불변식에 대응하는 Property Test.

---

### 4.3 View Properties

#### PT-VIEW-001: View switch purity

**Given** 동일 Snapshot
**When** compute INIT vs AVG5
**Then** INIT 계산 결과는 AVG5 계산 과정에 의해 변경되지 않는다

#### PT-VIEW-002: Avg5 bounds

**For all** values v1..v5
**Then** min(v) ≤ avg(v) ≤ max(v)

---

### 4.4 Negative-allowed Component Properties

#### PT-NEG-001: Cashback negativity allowed

**Given** CASHBACK included and value < 0
**Then** total_cost_amount may decrease but must still satisfy INV-CALC-002 (ratio definition)

---

### 4.5 Overflow / Large Number Properties

#### PT-OVF-001: Integer amount safety

**For all** price_per_unit, volume within configured bounds
**Then** revenue_amount multiplication does not overflow 64-bit integer (or system uses bigint)

---

### 4.6 Evaluation Properties

> **v1.3 추가:** v1.2에서 L3 Ontology Parity로 검증하던 평가 로직의 수학적 성질을 L2로 흡수.

#### PT-EVAL-001: Evaluation Completeness

**For all** metric_value M, target_value T, warning_threshold W
**Then** evaluateStatus(M, T, W) ∈ {GREEN, YELLOW, RED}

#### PT-EVAL-002: Evaluation Monotonicity

**For all** T, W fixed; M1 < M2
**Then** severity(evaluateStatus(M1, T, W)) ≤ severity(evaluateStatus(M2, T, W))

(where severity: GREEN=0, YELLOW=1, RED=2)

#### PT-EVAL-003: Evaluation Determinism

**For all** M, T, W
**Then** evaluateStatus(M, T, W) = evaluateStatus(M, T, W) (동일 입력 → 동일 출력)

---

### 4.7 Aggregation Properties

> **v1.3 추가:** 품번→차종 집계의 수학적 성질 검증.

#### PT-AGG-001: Aggregation Commutativity

**For all** Part ordering permutations
**Then** SUM(part_cost × usage) is independent of Part order

#### PT-AGG-002: Aggregation Non-negative

**For all** non-negative part costs, non-negative usage
**Then** aggregated vehicle cost ≥ 0

---

## 5. Test Data Generation (Fuzz)

* Generate random per-unit costs in [0..1e6]
* Generate random volumes in [0..1e7]
* Generate rates in [0..1]
* Generate customs in [0..1e4]
* Generate Multi CR rates in [0..0.10]
* Randomly toggle optional components(OFF/ON) with required allocation generation for Type C
* Generate random usage values in [1..10]
* Generate random part counts in [1..50]

All generated cases must satisfy:

* No NULLs
* PRICE > 0

---

## 6. Exit Criteria

* 모든 L1 Unit Test 통과
* L2 Property Test 최소 10,000 case fuzz 통과
* 불변식(08-A) 위반 0건

> **v1.3 변경:** v1.2의 L3(Ontology Parity Test 100% 일치) 조건 제거.

위 Exit Criteria를 전부 통과해야 계산 엔진의 정확성과 안전성이 확보된 것으로 본다.
